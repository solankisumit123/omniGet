[package]
name = "omnidisc-proto"
version = "0.1.0"
edition = "2021"
description = "Shared wire types, IDs and permission model for OmniDisc (client and server)"
license = "GPL-3.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_repr = "0.1"
thiserror = "1"

[dev-dependencies]
